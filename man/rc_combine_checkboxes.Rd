% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rc_combine_checkboxes.R
\name{rc_combine_checkboxes}
\alias{rc_combine_checkboxes}
\title{Combine Wide-Format Checkbox Fields}
\usage{
rc_combine_checkboxes(
  record_data,
  fields = NULL,
  data_dict = getOption("redcap_bundle")$data_dict,
  factor_labels = TRUE
)
}
\arguments{
\item{record_data}{Dataframe. Record data export from REDCap}

\item{fields}{Character vector. Names of checkbox fields to combine. If NULL
(default), all checkbox fields will be identified and combined. Field names
should be the root field name (without ___\link{code} suffix).}

\item{data_dict}{Dataframe. REDCap project data data_dictionary. By default,
this will be fetched from the REDCap bundle option, as created by \code{rc_bundle}.
Otherwise, a data.frame containing the project data dictionary must be supplied.}

\item{factor_labels}{Logical. Determines whether the combined values are
represented as numeric codes (FALSE) or option labels (TRUE). Default is TRUE.}
}
\value{
Dataframe with combined checkbox columns and wide-format columns removed.
}
\description{
Combines REDCap checkbox fields from wide format (multiple binary
columns with ___\link{code} suffixes) into a single column with comma-separated
values representing the selected options.
}
\details{
This function takes REDCap data in wide format where checkbox fields
are represented as multiple binary columns (e.g., cb_field___1, cb_field___2)
and combines them into a single column containing comma-separated values of
the selected option codes or labels. The wide-format columns are then removed
from the data frame.
}
\author{
Marcus Lehr
}
